{
  "scripts": {
    "build": "rm -Rf ./build && yarn run serverless package --package build"
  },
  "dependencies": {
    "tar": "^4.4.6"
  },
  "devDependencies": {
    "serverless": "^1.32.0"
  }
}
