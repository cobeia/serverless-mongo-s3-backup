{
  "scripts": {
    "build": "rm -Rf ./build && yarn run serverless package --package build"
  },
  "dependencies": {
    "archiver": "^3.0.0"
  },
  "devDependencies": {
    "serverless": "^1.32.0"
  }
}
